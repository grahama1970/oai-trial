# WebGPT adversarial peer review — ROUND 2 — 2026-09-04

Run: `/mnt/storage12tb/skills/ask/outputs/oai-trial-review/ask-tau-round-2-follow-up-in-this-same-c-1308af8a8a4e`. Handler PASS; verdict FAIL. Read the private repo main branch directly (authorized GitHub access), no attachment.

CONFIRMED_FIXED by the reviewer: prior #2, #3, #9, #1(CSV), #16(bool+dupkey), report-write atomicity. Remaining blockers below.

---

VERDICT: FAIL — The specific regressions are real, but the current `main` still has release-integrity, verifier-independence, protected-value, SQLite, scale/cost, and evidence-honesty defects that can produce a wrong READY corpus or leave required claims unsupported. fileciteturn90file0L2-L2 fileciteturn91file0L2-L2 fileciteturn92file0L2-L2

REMAINING_BLOCKERS:

1. **The verified bytes are still not bound to the published bytes.** `src/anonymization_trial/pipeline.py::run_pipeline,_publish`. `verify_corpus()` returns no digest or receipt. After it returns, `run_pipeline()` computes the *first* `sealed_digest`; therefore a mutation made after the verifier’s final read but before that digest is accepted, rechecked against itself, and published READY. A deterministic proof is a wrapper that calls the real verifier, then restores a sensitive literal before returning. `verification_sha256` is merely set equal to `corpus_manifest_sha256`, so it does not identify separate verification evidence. `test_publish_hardening.py` tests only successful final state and explicitly asserts this duplicate-digest design; it does not inject the remaining race. fileciteturn92file0L2-L2 fileciteturn94file0L2-L2

2. **Policy and source snapshot TOCTOU remains.** `src/anonymization_trial/pipeline.py::run_pipeline,_preflight,_source_digests`. `policy.json` is parsed before `_preflight` checks whether it is a symlink, then it is reread later to generate `policy_sha256`; a changed policy can therefore be used for compilation but a different policy can be named in the report. Corpus files are reopened by path for transformation, verification, and final hashing, while only the originally enumerated paths are rehashed. A swap-and-restore during those reads, or an added file after enumeration, is not excluded by the final equality check. `test_source_snapshot.py` only proves that two direct digest calls notice a persistent mutation; it does not exercise `run_pipeline`, a swap-and-restore race, policy mutation, inode replacement, or file-set mutation. fileciteturn92file0L2-L2 fileciteturn102file0L2-L2

3. **The verifier is still not independent and admits duplicate-key JSON containing sensitive data.** `src/anonymization_trial/verification.py::_expected_json,_verify_locations,verify_corpus` imports and calls the same `replace_text` matcher and `build_replacements` implementation used by the transformer. A common matcher or identity-selection defect can therefore transform and certify the same wrong mapping. Worse, verifier-side JSON uses ordinary `json.loads`, which silently keeps the last duplicate key. An output such as `{"who":"Alice","who":"<expected-pseudonym>"}` can be interpreted as only the second value by both `iter_searchable_text` and `_verify_locations`, allowing the raw sensitive first value to evade both absence and structural checks. Output verification must use a duplicate-rejecting parser and an independently implemented reference matcher. fileciteturn90file0L2-L2 fileciteturn91file0L2-L2

4. **SQLite can still mis-map identities and mutate unrelated data while passing verification.** `src/anonymization_trial/formats.py::_transform_sqlite,_verify_sqlite` selects and updates by bare `rowid`. In SQLite, a user-declared `rowid` column shadows the hidden row identifier. With duplicate declared `rowid` values, one row’s update can affect several rows. A three-row database can finish with both expected pseudonyms present somewhere, no original literal remaining, unchanged row counts, valid foreign keys, and `integrity_check=ok`, while individual identities are assigned to the wrong rows. Update triggers can similarly alter unrelated non-sensitive columns. `verification.py` has no SQLite location-by-primary-key recomputation, schema digest comparison, trigger check, or non-text-value oracle. This directly blocks the required SQLite relationship and meaning-preservation claim. fileciteturn90file0L2-L2 fileciteturn91file0L2-L2

5. **Partial protected/sensitive overlap still produces a READY protected-value violation.** `src/anonymization_trial/policy.py::_check_overlap` checks equality and containment only, despite `docs/ANONYMIZATION_SEMANTICS.md` promising rejection for partial prefix/suffix intersections. Concrete accepted case: sensitive `abcP`, protected `Person-`, source `abcPerson-`. Neither literal contains the other, but their source spans overlap. Replacing `abcP` with a name pseudonym beginning `Person-` changes the protected source span while preserving its aggregate count at one, so both the shared expected-output recomputation and protected occurrence-count test accept it. This is an input-only correctness failure, not a theoretical concurrent-write attack. fileciteturn86file0L2-L2 fileciteturn88file0L2-L2 fileciteturn120file0L2-L2

6. **Finite JSON numbers can still be silently changed.** `src/anonymization_trial/formats.py::_finite_float,_transform_json`. Rejecting infinity closes `1e400`, but every finite decimal still passes through binary `float`. For example, `9007199254740993.0` is re-emitted as `9007199254740992.0`, and sufficiently small exponents can underflow to `0.0`. The verifier parses the source through the same float behavior and accepts the changed output. That violates preservation of non-sensitive meaning. Use exact decimal/token preservation or explicitly reject values that cannot be represented without semantic change. fileciteturn90file0L2-L2

7. **Policy validation remains materially incomplete.** `src/anonymization_trial/policy.py::compile_policy` still defaults absent `sensitive_values` and `protected_values` to empty arrays even though the checked-in schema requires both. A schema-invalid policy can therefore become a successful no-op policy rather than fail closed. The implementation also does not validate an optional protected `reason` as a non-empty string or impose bounds on rule count, literal length, identifiers, or data types. The boolean-version and duplicate-key fixes do not close prior issue #16 as a whole. fileciteturn86file0L2-L2 fileciteturn121file0L2-L2

8. **CSV preservation is undefined and the same parser misconception is used for verification.** `src/anonymization_trial/formats.py::_transform_csv` and `verification.py::_csv_rows`. Both assume Python’s default comma/quote dialect without detecting, declaring, or rejecting alternatives. A semicolon-delimited file containing semicolon-dialect quoted commas can be parsed into the wrong columns and reserialized into a different logical dataset; the verifier repeats the default-dialect interpretation and approves it. Newline and quoting representation are also normalized. The trial allows a bounded format subset, but that subset must be defined and unsupported dialects rejected before release. fileciteturn90file0L2-L2 fileciteturn91file0L2-L2 fileciteturn125file0L2-L2

9. **The pseudonym and privacy contract is still overclaimed.** `src/anonymization_trial/pseudonyms.py` has one fixed public `SCOPE_ID`, a 253-value IP namespace, and a 10,000-value phone namespace. There is no cardinality preflight; collision search can run up to 100,000 attempts. `docs/ANONYMIZATION_SEMANTICS.md` claims domain extension and a second IP range that the code does not implement. `docs/PRIVACY_CONTRACT.md` says cross-tenant linkage is disallowed and assumes the release consumer lacks a private key, but the local algorithm has no private key and produces identical subject/type tokens across every corpus using `trial-v1`. Multi-tenancy is not required for the trial, but the false unlinkability and key assumptions must be removed or implemented before the privacy claims are acceptable. fileciteturn88file0L2-L2 fileciteturn100file0L2-L2 fileciteturn120file0L2-L2

10. **The TB/PB design, SLA, and cost arithmetic do not satisfy the brief.** The local implementation still materializes text/JSON bytes, all SQLite rows, all Aho matches, and the corpus-wide searchable text list, while `SUBMISSION.md` says TXT and CSV stream. Production proposes “line-aligned” CSV splitting, which is unsafe for quoted multiline records, and promotes verified files individually into a release bucket without a corpus manifest or atomic active pointer, so readers can observe a partial corpus. Its SLA assumes 200 workers at 20 MB/s each: that is 4 GB/s, making 1 TB about 250 seconds before overhead—not the stated 1.4 hours—and it gives no 1 PB completion SLA. The estimator prices only two month-long S3 copies, one GET, one PUT, and one compute pass; it omits the source and staged verifier rereads, staging/promotion requests, output expansion, failed retries, KMS, SQS, EventBridge, orchestration, logs/audit, quarantine, transfer assumptions, and S3 price tiers. The `workers` input is unused by the cost calculation. fileciteturn90file0L2-L2 fileciteturn91file0L2-L2 fileciteturn98file0L2-L2 fileciteturn105file0L2-L2 fileciteturn106file0L2-L2 fileciteturn107file0L2-L2

11. **The advertised security lineage remains partly theater.** The `$hack` receipt reports Semgrep PASS despite scanning zero target files; it contains Bandit results but no dependency-SCA execution, while README claims Semgrep, Bandit, and SCA. The “black-box” tests invoke the source tree through `PYTHONPATH` and the local Python interpreter rather than testing the built Docker image using only its mounted contract. The Battle run identifies itself as a non-agentic deterministic fixture and explicitly disclaims real Red/Blue behavior and multi-round learning; its listed supporting artifacts are absent from `security/battle/`. The Judge declares `functional_evidence_status="INSUFFICIENT_EVIDENCE"` while still reporting `BLUE_SUCCESS`. These artifacts may prove fixture wiring, but not adaptive security hardening of `oai-trial`. fileciteturn113file0L2-L2 fileciteturn115file0L2-L2 fileciteturn109file0L2-L2 fileciteturn110file0L2-L2 fileciteturn112file0L2-L2

12. **The evidence and honesty surfaces still contradict one another.** `README.md` reports 62 tests; `SUBMISSION.md` reports 66; the round-two request reports 74. `security/ADVERSARIAL_MATRIX.md` still labels newly added tests as pending and describes “VERIFIED” cases as `xfail(strict)`, although the cited tests are now green. `docs/ACCEPTANCE_MATRIX.md` claims chunk-boundary replacement, structural-fingerprint parity, SQLite schema hashes, verifier BOM checks, and extra-artifact rejection that are not implemented by the reviewed code. The privacy contract labels every occurrence, full SQLite structure, and atomic release “proven” despite the blockers above. Finally, `errors.py` promises messages never contain attacker-controlled identifiers, while `policy.py` interpolates `rule_id` and `data_type`; the CLI suppresses the message, but the blanket library/log claim is still false. This level of claim drift blocks a work-trial PASS even where the underlying mechanism has improved. fileciteturn89file0L2-L2 fileciteturn86file0L2-L2 fileciteturn96file0L2-L2 fileciteturn98file0L2-L2 fileciteturn99file0L2-L2 fileciteturn100file0L2-L2 fileciteturn122file0L2-L2

CONFIRMED_FIXED:

1. **Prior #2: the escaped-JSON literal bypass is closed for ordinary duplicate-free JSON.** `iter_searchable_text` now traverses decoded keys and string values, and `test_escaped_json_sensitive_value_rejected` proves that `"\u0041lice"` is detected rather than scanned only as serialized bytes. fileciteturn90file0L2-L2 fileciteturn95file0L2-L2

2. **Prior #3: the specific `1e400 → Infinity` invalid-JSON defect is closed.** `_finite_float` rejects overflow to non-finite values, `json.dumps(..., allow_nan=False)` prevents invalid output, and the retained regression exercises that case. This does not resolve finite-number precision loss described above. fileciteturn90file0L2-L2 fileciteturn93file0L2-L2

3. **Prior #9: equal case-insensitive match domains assigned to different identities are rejected.** `compile_policy` now compares ASCII-lowered domains and raises `IDENTITY_CONFLICT`; the retained `Alice`/`ALICE` regression demonstrates the intended rejection. fileciteturn86file0L2-L2 fileciteturn93file0L2-L2

4. **The specific CSV swap and dropped-row examples from prior #1 are closed.** `_verify_locations` compares row count, column count, and each source/output cell, and both mutations are covered by retained tests. I do not consider prior #1 resolved globally because SQLite is not location-bound and the verifier still shares the production matcher. fileciteturn91file0L2-L2 fileciteturn95file0L2-L2

5. **Two sub-defects from prior #16 are fixed.** `version:true` no longer passes as integer version 1, and duplicate keys in `policy.json` are rejected by `object_pairs_hook`; both have focused regressions. Prior #16 remains open because required arrays and the rest of the schema are not enforced. fileciteturn86file0L2-L2 fileciteturn93file0L2-L2

6. **The report-file write primitive is materially improved.** `_publish` now invalidates the previous readiness marker before corpus replacement and writes the new report through a same-directory temporary file, `fsync`, `os.replace`, and directory `fsync`. This fixes the narrow non-atomic `Path.write_text()` defect, but does not resolve the verify-to-seal race, concurrent-writer race, or lack of fault-injected crash evidence listed above. fileciteturn92file0L2-L2
