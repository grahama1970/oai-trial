# Coordinated authoring revision

## What changed

- Replaced the seven-slide limit with **28 prepared slides + Discussion + Thank you**. The first slide is Table of Contents. No cover precedes it and no substantive playback slide follows Thank you.
- Kept **30 minutes total prepared time including navigation**, plus a separate **15+ minute audience reserve**. The 28 prepared slots are deliberate rehearsal allocations, not measured speaking durations.
- Moved the recorded four-format result and observed demo timing/memory ahead of architecture. The opening distinguishes checker-asserted synthetic values from actual raw output captures.
- Separated Docker/setup, the actual mounted CLI, and independent output evidence into reproduction pages.
- Used one compact pipeline map, followed by one-concept policy, identity, span, format, typed/location and publication pages. All use the same Alice/A.L/person-a and Bob/person-b fixture context, with KEEP and typed values as controls.
- Kept AWS architecture, capacity/SLA and cost assumptions in the required core—not Extra Credit. The cost figure compares the four components **within 1 PB**, with explicit modeled units. No pooled TB/PB histogram or meaningless pooled mean is used.
- Added three explicit standalone prepared **Question → Answer pairs**, immediately before Extra Credit. They defend exact policy, shared-primitives verification, and the scale/model boundary. These are not the later audience discussion.
- Made Extra Credit the final substantive prepared block. Its TOC uses exactly **Security Evals (White, Grey, Black, and Adaptive Lineage)**, followed by the thin wrapper and reviewed aliases. Detail slides qualify white/gray/black-box methodology, Bandit-only supporting evidence, zero-target Semgrep, no SCA receipt, and the fixture-backed Battle/Judge demonstration.
- Included the corrected canonical-path boundary at the end of the reviewed alias workflow. It remains a post-trial addition under the existing trust assumptions, not a new security-review round.
- Preserved Q01–Q48 and all existing question/short-answer/follow-up/reference text verbatim. The new primary-slide mapping and legacy section IDs are retained separately. The entire question bank is Markdown/reference data, not normal deck playback.
- Applied the supplied grahama.co tokens: Fraunces display/headings, system sans prose, and monospaced machine identifiers in code/identifier SVGs. The ACTUAL supplied header PNG is included byte-for-byte. The supported theme uses it at 10% image opacity over an independently opaque brown fill. No fake translucent rectangle or duplicated footer elements were added.
- Authored self-contained static diagrams and a data-derived bar chart. These are explicitly illustrations/figures, not terminal screenshots, debugger captures, execution evidence or native PowerPoint internal objects.
- Reset all authored claims to **candidate** with exact source passages. No prior approvals, approval timestamps, or historical approval hashes were carried forward.

## Source and evidence boundaries

The baseline remains `0375af56bf681e9441edcb7433cfe58951db77b2`. The supplied full reviewed narrative, revised outline, evidence, schemas, design guidance and reference sheet were read. Selected code windows and the retained Battle receipt were inspected through the authorized GitHub connector; see `sources/source-access.json`. The current consumer's header/font behavior was inspected only to avoid unsupported authoring fields. Its separately pinned consumer commit is not an implementation version of the anonymizer.

The qualification receipt is an actual supplied **historical** artifact. Its demo observations and readback statuses were not remeasured. The original trial ZIP and raw command logs were not supplied for new execution or rehash. The authored fixture diagram displays the qualification checker's expected values and is explicitly labeled as such. The earlier bounded reviewer PASS is a source-review conclusion, not execution personally performed here.

The cost rows and source outputs are **modeled**. Supplied analytics describe output was inspected; analytics and create-figure were not run. The chart input and optional reproduction command are retained with `NOT_RUN` status in the figure spec. Its rounded four component values sum to $85,733.91 while the committed total is $85,733.90 from unrounded terms; this one-cent display distinction is preserved, not silently adjusted.

## Validation actually performed

- Parsed all authoring JSON and validated deck, claim ledger, source manifest and asset manifest with the exact supplied exported JSON Schemas using Python jsonschema.
- Checked slide order/IDs, 30-minute prepared timing, separate audience reserve, exactly three prepared question/answer pairs, and the placement of Extra Credit/Discussion/Thank you.
- Checked all 48 question mappings, compared the preserved question and answer texts to the supplied source, and checked original Markdown reference links.
- Checked complete native-text bindings, candidate-only claim states, source/claim/asset reference resolution, actual local source/asset existence, exact source evidence passages, no authored footer elements and bounded freeform element geometry.
- Parsed SVG XML and checked for scripts, foreignObject and external references. Rasterized the non-navigation SVG assets with CairoSVG and inspected their contact sheet. This is asset-level authoring inspection, not the owning create-svg validator or a rendered deck approval.
- Verified that the header PNG matches the supplied bytes/hash, that house tokens match the supplied preset, and that no font files are in the bundle.
- Ran a sans-metric native-text fit estimate. It found no overflow warnings, but it is not a substitute for actual Fraunces/PowerPoint/browser rendering.

The actual check record is `validation/report.json`. A fresh package inventory is in `PACKAGE-MANIFEST.json`; these are authoring-file checks, not anonymizer qualification receipts.

## Not run / unresolved acceptance

1. The local pitchdeck build/verify/publish compiler, owning SVG validators, analytics/create-figure tools, browser theme compositor, PPTX/PDF export, Google Slides import and human preview were **not run**. Claims remain candidate. Local render/inspection is still required before delivery.
2. Fraunces is not installed in this authoring container. SVG asset inspection could use fallback fonts. The owning consumer supplies its supported font setup; confirm actual heading/font wrapping and the real 10% header image composition in that environment. No font binaries are distributed.
3. The consumer automatically uses its canonical house-band texture. The packet's byte-identical copy is included as provenance/handoff; do not add a second header-image element. The extracted header hash and supported tokens are in `theme/README.md`.
4. The original frozen qualification script records its invoking HEAD, clones main and requires equality. Re-running it after main moves is not equivalent to the historical receipt. No retrospective execution claim is made here.
5. No new unresolved technical-runtime question was opened. Timebox overruns, unfinished work and post-trial additions remain disclosed. This presentation is not a claim of eight-hour compliance or production completeness.

## Explicit non-integrations

No Archify, React Flow, general-purpose create-gsn assurance case, debugger frame/capture, voice capture, Memory import, Live Evidence delivery or recording consent was created or established. Formal-assessment restrictions remain explicit. This package is presentation authoring and preparation only.

## Local integration after the WebGPT handoff

The downloaded original ZIP is retained unchanged outside the repository. This
copy includes bounded local corrections: three missing visible qualifiers on
prepared-question slides; native editable question text and source-navigation
labels instead of text-only SVGs; an explicit 1000/100 = 10 source derivation;
and the verified input_bundle end line (71). The transcript qualifiers and
source references were kept in sync. No runtime source changed.

The local consumer models and draft build passed. All SVG assets passed the
owning create-svg XML/safety checks. This remains an unapproved presentation
draft: Google Slides import, full visual approval, live-demo rehearsal,
VS Code interaction, and Live Evidence wiring are not established. The opening
currently uses recorded qualification evidence, not a newly captured live run.
Some picture containers retain legacy blue framing; the visual review must not
be represented as final grahama.co conformance.

The original validation/report.json records WebGPT's authoring checks before
these local changes; it is not the local renderer's acceptance receipt.
