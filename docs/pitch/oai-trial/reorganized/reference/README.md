# Prior preview — context only

`prior-draft-preview.png` is copied from the incoming packet. It precedes the latest notes and this focused revision. It was visually inspected as context, not generated here or approved. In particular, it cannot establish the changed wrapper slide, notes synchronization, or final renderer framing. It is not a deck image asset or execution screenshot.
