# Focused interview/slides update — candidate

Input presentation commit: `ecfaaaac2cc7844bd0e52fd7d2bbf3abab36ab78`. Unchanged anonymizer baseline: `0375af56bf681e9441edcb7433cfe58951db77b2`. This updates the committed package; it is not a restart or a runtime/security revision.

## Substantive changes

- Trimmed three inherited TOC text-box heights that overlapped neighboring headings; hierarchy, words and timing remain intact. Final text fit still requires the local renderer.
- Kept all 30 playback IDs/order: 28 prepared slides total exactly 30 minutes; Discussion has a separate 15+ minute reserve; Thank you remains last. No skill introduction slide is added.
- Coordinated the r01/r02 demo plan across TOC, transcript, notes and map. The sole live pre-command wrapper introduction is the operator's exact sentence. Preflight occurs before playback; live success language requires actual output inspection. Recorded fallback and historical metrics are explicit alternate evidence, not fresh measurements.
- Added `INTERVIEW_GUIDE.md`: concise preflight, timed demo branches, rehearsal cues and direct links into the preserved Q01–Q48 appendix. No fabricated interview dialogue or extra prepared talk time.
- Changed only the r26 substantive visible composition: three native editable rows for concise SKILL.md contracts, thin run.sh delegation and retained behavior checks for reusable workflows. Source references are explicit; no live skill build/install or blanket compliance claim.
- Synchronized deck notes from the revised transcript, including the three existing visible prepared-question qualifiers and input_bundle L40–L71. Kept native question/source-navigation elements and removed obsolete image bindings that could restore text-only SVGs. No authored footer was introduced.
- Preserved all 48 question/short/deeper answers and primary-slide mappings byte-for-byte in `question-map.json`. Added stable question anchors to the Markdown appendix without changing answers.
- Corrected six candidate evidence spans that had accidentally matched question-handle headings instead of their actual answer rows. No new assertion or approval was substituted.
- Reused the actual assets, historical demo values, modeled cost data and chart specifications. Corrected the remaining L40–L74 label on an unused source-navigation SVG to L40–L71. No chart was remeasured or regenerated.
- Kept the grahama.co tokens and actual house-band PNG unchanged: independent opaque brown fill and 0.1 image overlay remain consumer-owned. All claims remain candidate.

## Validation performed here

See `validation/authoring-checks.json` for checks actually run and `validation/SCOPE.md` for their limits. Incoming payloads were decoded and compared to their own supplied SHA-256 entries. Supplied JSON Schemas, reference relationships, timing, question preservation, native text, visible qualifiers, source passages, asset paths and file packaging were checked. Source access is recorded separately; the provided preview was inspected as historical context.

## Unverified acceptance

This revised deck has not run through the local pitchdeck producer/compiler, owning SVG validators, create-figure, Google Slides/import, or human approval. The project agent's earlier local model/build/report results remain reported historical work. Final typography, wrapping, actual header composition and legacy blue picture framing need local renderer/GUI inspection. No shared renderer code was changed. The demo itself still needs a permitted presenter preflight/rehearsal; no current run, screenshot or measurement is invented.

One receipt metadata ref could not be fetched as an agent-skills commit. It is not used as verified byte provenance; the new reuse excerpts identify the known earlier source snapshot separately. See `validation/source-access-update.json`. This does not change or recreate historical execution evidence.

No application, pytest, Ruff, Docker, project-state, analytics, live integration, recording, VS Code sync or debugger execution was performed. Technical review remains distinct from timebox compliance and assessment permissions. Earlier changelog: `validation/prior-changelog.md`.

## Local integration correction

The rehearsal guide now keeps the primary checkout on main and compares runtime
files against the frozen baseline. Later presentation/documentation commits do
not require a checkout change. The original downloaded ZIP remains unchanged.
Local consumer model/build checks have run; final rendered/interactive approval
remains separate.
